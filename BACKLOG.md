# 📋 프로젝트 백로그 (BACKLOG.md)

이 문서는 프로젝트의 전체 작업 목록(Task)과 상태, 그리고 주요 의사결정 사항을 추적하는 공간입니다.
AI 에이전트 간 Git 충돌(Merge Conflict)을 방지하고 가독성을 높이기 위해 '진행 단계' 및 '상태'별 리스트 형식으로 관리합니다.

---

## 🏷️ 분류 정의 (Category Definitions)
- **[문서/기타]**: 프로젝트 가이드라인, 백로그 관리, 설계 기록 등.
- **[인프라/공통]**: 라이브러리 설치, CI/CD, 라우팅 기초, 공통 레이아웃.
- **[컴포넌트]**: UI 컴포넌트(커버, 갤러리, 방명록 등) 개발.
- **[데이터/API]**: Supabase 연결, 스키마 정의, API 통신.
- **[어드민/빌더]**: 관리자 대시보드, 청첩장 생성 폼, 인증 처리.

---

## 🔄 1. 최근 작업 인수인계 (Handoff)
*새로운 에이전트가 작업을 시작하기 전 반드시 읽어야 할, 이전 에이전트의 마지막 작업 맥락입니다.*

- **마지막 작업 내용**: 프로젝트 전반을 8개 관점(접근성, 보안, 콘텐츠 가이드 준수, 엔지니어링 건전성, 어드민 워크플로우, 실기기 성능, React 동작 버그, 제품 기능 공백)에서 점검하고, 도출된 항목을 아래 대기열에 WB-043 ~ WB-058로 등록했습니다. 함께 **PR #58(테마별 스크롤 등장 애니메이션)을 원복**했습니다 — 머지 후 실측해보니 해당 애니메이션이 한 번도 재생되지 않았습니다(트리거가 페이지 최하단이라 도달 시점엔 `once: true`로 전 섹션이 이미 등장 완료). 의도는 WB-055로 남겼습니다.

- **점검에서 확인된 주요 사실** (추측이 아니라 실행/측정으로 확인한 것만):
  - **해외 하객에게 예식 날짜와 요일이 틀리게 표시됨**. 빌더가 `toISOString()`으로 저장하고 하객 화면이 각자 폰 시간대로 포맷하기 때문. KST 11/14(토) 12:30 예식이 LA에서는 `2026. 11. 13. 금요일 7:30 PM`으로 보입니다. → WB-045
  - **방명록은 비밀번호 없이 누구나 전부 삭제 가능**. 검증이 브라우저 안에서만 일어나고 DB 제약이 없습니다(`useGuestbook.ts:52` 주석이 이를 명시). 비밀번호는 평문이며 익명 조회도 가능합니다. → WB-043
  - **골드 `#D4AF37`의 배경 대비율이 2.00:1** (WCAG 기준 4.5:1). 모든 섹션 제목이 이 색입니다. → WB-050
  - **`vite.config.ts:22`의 `manualChunks`가 게스트/어드민 클라이언트 분리를 무효화**하고 있습니다. 소스 코드는 아래 '프로젝트 결정 사항'의 분리 규칙을 정확히 지키고 있으나, 설정이 `@supabase/*`를 한 덩어리로 묶어 auth·realtime·storage까지 하객 번들에 싣습니다(49.7KB gz → 정정 시 4.6KB gz). → WB-051
  - 레포에 **스키마/RLS 정의 파일이 하나도 없어** 보안 구성을 코드만으로는 검증할 수 없습니다. slug UNIQUE 제약 여부도 확인 불가이며, 없을 경우 중복 시 `.single()`이 `PGRST116`을 반환해 **두 청첩장이 동시에 "찾을 수 없습니다"가 됩니다.** → WB-044
  - `docs/CONTENT_STYLE_GUIDE.md`의 **승인 템플릿 5종 중 실제 코드에 쓰인 것은 0건**입니다. → WB-057

- **마지막 작업 내용**: 재미 요소로 청첩장 최하단에 숨겨진 뷰 모드 2종을 추가했습니다(WB-042). "개발자 버전"은 DOS 부트 시퀀스 배너 + 초록 터미널 스킨, "디자이너 버전"은 무지개 배경 + Comic Sans + 마퀴 배너 + 커서 반짝이 효과로 청첩장 전체를 리스킨합니다. 실제 청첩장 정보(이름, 날짜, 갤러리 등)는 그대로 유지한 채 시각적 스킨만 바뀝니다. `src/styles/prankModes.css`가 `--color-*`/`--font-family-*` CSS 변수를 재정의하는 방식으로 대부분의 하위 컴포넌트를 건드리지 않고 리스킨합니다. `src/components/EasterEgg/`.

- **마지막 작업 내용**: 디자인에서 목적 없이 무한 반복되는 장식 애니메이션을 점검해 제거했습니다. `Calendar` 컴포넌트에서 예식일 숫자 뒤의 하트 마크가 2초 주기로 영원히 커지고 작아지는(pulse) 애니메이션이었는데, 어떤 상태 변화도 나타내지 않는 순수 장식이라 정적인 워터마크로 바꿨습니다(`Calendar.module.css`). 나머지 코드베이스(그림자, 색상, 버튼 동작, 이모지 사용)는 점검했으나 특이 사항 없었습니다.

- **마지막 작업 내용**: 성능·가독성 관점의 리팩토링 PR 6건이 순차적으로 머지되었습니다. 카카오 SDK 온디맨드 로딩 및 타입 통합(PR #46), 테마 폰트 선택적 로딩 및 커버 이미지 우선순위(PR #47), 인라인 스타일·중복 코드·CSS 색상 변수 정리(PR #48), keep-alive 워크플로우가 `VITE_` 접두사 시크릿도 인식하도록 수정(PR #49), 하객 뷰 초기 번들 재작업 — 라우트/컴포넌트 단위 lazy loading 및 벤더 청크 분리(PR #50, WB-035), 그 과정에서 깨진 Vercel 빌드 긴급 수정(PR #51)입니다.

- **🔴 미완료 (사람 조치 필요, 긴급)**: keep-alive 워크플로우가 8/7~8/10 나흘간 정상 작동하다가 **8/11 실행에서 다시 `curl: Could not resolve host` (DNS 실패)로 실패**했습니다. 시크릿은 정상 인식되고 있어(PR #49로 확인됨) 원인은 시크릿 문제가 아닙니다. 나흘 연속 정상이던 것이 재발했다는 점에서 단순 7일 비활성 정지가 아니라 다른 원인(수동 일시정지, 프로젝트 재발급으로 URL 변경, Supabase 측 DNS 인프라 이슈 등)일 수 있습니다. **Supabase 대시보드에서 프로젝트 상태를 직접 확인**한 뒤, Actions → Supabase Keep-Alive → Run workflow로 재확인이 필요합니다.

- **⚠️ 미완료 (판단 필요)**:
  1. **강조색 2종 혼용** — WB-050으로 편입. 점검 결과 `#d4a373` 하드코딩 7곳에 더해 `InvitationPage.module.css:17`의 `#D4AF37` 하드코딩까지 **총 8곳**입니다. 대비율 문제(2.00:1)와 함께 판단해야 합니다.
  2. **`InvitationBuilder.tsx` 인라인 스타일 10곳** — 점검 결과 10곳이 맞습니다(254, 265, 284, 308, 309, 312, 322, 325, 365, 368). 진행 가능합니다.

- **다음 작업 제안**: 대기열의 🔴 표시 항목(WB-043, WB-044)은 **Supabase 대시보드 확인이 선행되어야** 코드 작업을 마칠 수 있으므로 사람의 조치가 먼저 필요합니다. 코드만으로 완결 가능한 것 중에서는 **WB-046(요청 타임아웃) → WB-047(ErrorBoundary) → WB-048(RSVP 내보내기)** 순서를 권합니다 — 셋 다 작고 서로 독립적이며 예식 당일 리스크를 직접 줄입니다.

---

## 🗂️ 2. 진행 중 & 대기열 (Active & Backlog)

### 🏃‍♂️ 진행 중 (In Progress)
- (현재 진행 중인 작업 없음)

### 📝 대기열 (To-Do)

*아래 우선순위는 "실제 예식 당일에 무엇이 잘못되는가"를 기준으로 매겼습니다.*

#### 🔴 0순위 — 사람 조치 선행 필요 (Supabase 대시보드)
- `[ ]` **[WB-043]** **[데이터/API]** 방명록·RSVP RLS 정책 정비
  - 현재 방명록 삭제는 브라우저 안에서만 검증되고 DB 제약이 없어 **누구나 REST 호출로 전체 삭제 가능**. 비밀번호는 평문이며 익명 `select`로 조회됨. RSVP에는 하객 전화번호가 들어가므로 anon `SELECT`가 열려 있으면 명단 전체가 유출됨.
  - 조치: anon DELETE 차단 → 비밀번호 검증을 `SECURITY DEFINER` RPC로 이전, 비밀번호 해시화 및 컬럼 조회 권한 회수, RSVP anon SELECT 차단.
- `[ ]` **[WB-044]** **[데이터/API]** `invitations.slug` UNIQUE 제약 확인 및 스키마 파일 레포 반영
  - 레포에 스키마/RLS 정의가 전혀 없어 제약 존재 여부를 코드로 확인할 수 없음. 제약이 없으면 slug 중복 시 `.single()`이 `PGRST116`을 반환하고 `useInvitationData.ts:41`이 이를 `notFound`로 매핑해 **두 청첩장이 동시에 404**가 됨.

#### 🟥 1순위 — 예식 당일 치명적 (코드로 해결 가능)
- `[ ]` **[WB-045]** **[데이터/API]** 예식 일시의 시간대 처리 정정 — 해외 하객에게 날짜·요일이 하루 밀려 표시됨
- `[ ]` **[WB-046]** **[데이터/API]** Supabase 요청 타임아웃 도입 — 응답이 지연되면 스켈레톤이 영구 유지되고 이미 구현된 재시도 버튼에 도달할 수 없음
- `[ ]` **[WB-047]** **[인프라/공통]** ErrorBoundary 추가 및 JSONB 널 방어 — `groomParents.mother` 누락 등으로 청첩장 전체가 백지가 됨
- `[ ]` **[WB-048]** **[어드민]** RSVP 명단 내보내기(CSV/복사) — 식장 인원 통보 마감을 손으로 옮겨 적어 대응 중

#### 🟧 2순위 — 하객 경험 직접 타격
- `[ ]` **[WB-049]** **[컴포넌트]** 커버·갤러리 이미지 CDN 변환 + `srcset` — 원본(수 MB)을 그대로 받아 480px에 그림
- `[ ]` **[WB-050]** **[컴포넌트]** 강조색 통일 및 대비율 개선 — 골드 `#D4AF37` 대비율 2.00:1, 하드코딩 8곳
- `[ ]` **[WB-051]** **[인프라/공통]** `manualChunks`를 패키지명 정확 매칭으로 수정 — 게스트/어드민 분리 원칙이 번들 단계에서 무효화됨
- `[ ]` **[WB-052]** **[어드민/빌더]** 혼주 계좌 입력란 추가 — 화면(`Account.tsx`)은 이미 렌더링하는데 입력 수단이 없어 항상 빈 값
- `[ ]` **[WB-053]** **[컴포넌트]** 오시는 길 정보 확장 — 주차·대중교통·셔틀 안내, 층/홀(`detailAddress`)과 식장 대표번호(`contact`) 노출 (타입에는 이미 존재하나 미사용)

#### 🟨 3순위 — 재발 방지 및 품질
- `[ ]` **[WB-036]** **[인프라/공통]** PR 단계 빌드/린트 검증 CI 추가 (현재 `main` push에만 실행). `npm install`이 아닌 **`npm ci`**로 실행해야 PR #50/#51 유형의 사고를 잡을 수 있음
- `[ ]` **[WB-054]** **[인프라/공통]** 테스트 도입 — Playwright(390px 가로 넘침 가드, 네트워크 차단 시 재시도 노출, 전 섹션 스모크) + Vitest(매퍼, `middleware.ts` 순수 함수)
- `[ ]` **[WB-056]** **[컴포넌트]** 접근성 정비 — 모달 3종 중 2종이 Escape/포커스 트랩 미처리, 갤러리가 키보드로 접근 불가, `prefers-reduced-motion` 미지원, `index.html`이 `lang="en"`, RSVP 폼 label 미연결, 터치 타깃 44px 미만 다수
- `[ ]` **[WB-057]** **[문서/기타]** `alert()` 6곳 제거 및 콘텐츠 스타일 가이드 승인 템플릿 적용 — 가이드의 승인 템플릿 5종 중 실제 사용 0건, 금칙어(`실패`/`오류`/`서버`)가 하객 화면에 노출 중. 가이드 자체에도 alert 금지·검증 에러 템플릿·성공 피드백 의무 규칙 추가 필요
- `[ ]` **[WB-058]** **[어드민/빌더]** 어드민 운영 기능 보강 — 갤러리가 콤마로 이어붙인 URL 한 덩어리(파일 업로드 없음), 삭제/비공개/미리보기 부재로 저장 즉시 공개, 폼 이탈 시 35개 필드 전량 소실, 방명록 어드민 관리 불가

#### 🟩 4순위 — 소규모 정리
- `[ ]` **[WB-059]** **[인프라/공통]** 잡항목 묶음 — 매퍼 중복(`AdminDashboard.tsx` 사본에 `themeFont` 누락), `ShareSection.tsx:14` 공유 링크 도메인 하드코딩, `<img>` `onError` 폴백 부재, 죽은 에셋 4개 및 미사용 타입 필드 정리
- `[ ]` **[WB-055]** **[컴포넌트]** 테마별 스크롤 등장 애니메이션 재구현 (PR #58 원복분)
  - 원복 사유: 트리거가 페이지 최하단이라 도달 시점엔 `once: true`로 전 섹션이 이미 등장 완료 → 애니메이션이 재생되지 않음.
  - 재시도 시 조건: 리마운트 방식은 금지(하객이 입력 중이던 방명록 내용이 사라짐). **WB-049/WB-051의 FadeIn·LCP 재작업 이후**에 진행할 것.
- `[ ]` **[WB-020]** **[컴포넌트]** 로맨틱 배경음악(BGM) 플로팅 플레이어 추가
  - **우선순위 하향 권고**: 청첩장은 대부분 음소거 상태로 열리고 모바일 브라우저가 자동재생을 차단하므로, 대기열 항목 중 값어치가 가장 낮음.

---

## 🗄️ 3. 아카이브 (Done)

### ✅ 완료 (Phase 5: 안정화 및 신규 기능 고도화)
- `[x]` **[WB-062]** **[컴포넌트]** 테마별 스크롤 등장 애니메이션 (PR #58) — **추가 후 원복됨**
  - 연관: WB-042, WB-055 / 완료일: 2026-09-17
  - 비고: 머지 후 실측 결과 애니메이션이 한 번도 재생되지 않아 원복. 트리거가 페이지 최하단이라 도달 시점엔 `viewport={{ once: true }}`로 전 섹션이 이미 등장을 마친 상태였음. 재구현 조건은 WB-055 참고.
- `[x]` **[WB-061]** **[인프라/공통]** SessionStart 훅으로 git identity 영구 고정 (PR #56)
  - 연관: - / 완료일: 2026-09-17
  - 비고: 원격 실행 환경은 세션마다 컨테이너가 새로 생성되어 로컬 git config가 초기화되므로, 커밋 author가 플레이스홀더로 찍히던 문제. `.claude/hooks/session-start.sh`가 매 세션 시작 시 실제 작업자 계정으로 재설정. AI 협업 사실은 각 커밋의 `Co-Authored-By` 트레일러로 계속 남음.
- `[x]` **[WB-060]** **[컴포넌트]** 방명록 섹션 모바일 가로 스크롤 버그 수정 (PR #57)
  - 연관: WB-008 / 완료일: 2026-09-17
  - 비고: 390px 뷰포트에서 페이지 스크롤 폭이 435px까지 벌어져 입력창과 등록 버튼이 잘렸음. 원인 두 가지 — ① `.container`(`width:100%; max-width:480px`)를 className 없는 `<FadeIn>`이 감싸 퍼센트 폭이 해석되지 못하고 `max-width` 값 그대로 렌더링됨 ② `<input>`은 flex item의 `min-width` 기본값이 `auto`라 `flex:1`이어도 고유 최소 폭 아래로 줄지 않음(`min-width: 0`으로 해결). **`max-width`를 가진 요소를 `FadeIn`으로 감쌀 때 반복될 수 있는 유형이므로 주의.**
- `[x]` **[WB-042]** **[컴포넌트]** 숨겨진 이스터에그 뷰 모드 (개발자/디자이너 버전) 추가
  - 연관: - / 완료일: 2026-08-23
  - 비고: `src/components/EasterEgg/`가 청첩장 최하단에 트리거 버튼을 노출. 클릭 시 `src/styles/prankModes.css`가 `--color-*`/`--font-family-*` 변수를 재정의해 실제 컴포넌트 코드를 건드리지 않고 전체를 리스킨. 개발자=DOS 터미널 스킨(부트 배너, 스캔라인), 디자이너=90년대 웹 스킨(무지개 배경, Comic Sans, 마퀴 배너, 커서 반짝이). 텍스트 가독성은 반투명 카드 대신 흰색 외곽선(text-shadow)으로 확보 — 카드형 오버레이는 글래스모피즘이 되어버려 지양.
- `[x]` **[WB-026]** **[컴포넌트]** 혼주 및 신랑신부 연락처 (전화/문자) 연결 모달 추가
  - 연관: WB-009 / 완료일: 2026-08-19
  - 비고: ContactModal 컴포넌트 추가 및 tel/sms 링크 연동, 어드민 빌더 혼주 전화번호 입력 폼 확장.
- `[x]` **[WB-041]** **[인프라/공통]** Vercel 빌드 에러 긴급 수정 (PR #51)
  - 연관: WB-035 / 완료일: 2026-08-07
  - 비고: PR #50이 `package.json`에 기록하지 않은 의존성을 `vite.config.ts`에서 import해 로컬은 통과했지만 Vercel 클린 설치에서 빌드가 깨졌음. 의존성 제거 및 Node 20 -> 22 승급으로 해결.
- `[x]` **[WB-035]** **[인프라/공통]** 하객 뷰 초기 번들 성능 최적화 (Lazy Loading & Chunk Splitting) (PR #50)
  - 연관: - / 완료일: 2026-08-05
  - 비고: 메인 청크 407KB -> 14KB로 대폭 축소 및 벤더 분리. 후속 빌드 에러는 WB-041에서 수정.
- `[x]` **[WB-040]** **[인프라/공통]** keep-alive 시크릿 이름 호환성 수정 (PR #49)
  - 연관: WB-031 / 완료일: 2026-08-04
  - 비고: 레포 시크릿을 Vercel과 동일하게 `VITE_` 접두사로 등록해도 인식하도록 `secrets.SUPABASE_URL || secrets.VITE_SUPABASE_URL` 형태로 수정. `middleware.ts`와 동일한 방식.
- `[x]` **[WB-039]** **[문서/기타]** 코드 일관성 리팩토링 (PR #48)
  - 연관: - / 완료일: 2026-08-04
  - 비고: `db.ts`/`supabase.ts`의 중복 환경 변수 읽기를 `supabaseEnv.ts`로 분리. `AdminDetail.tsx` 인라인 스타일 5곳을 CSS 모듈로 이동. `var(--color-primary, #d4a373)` 처럼 실제 변수값(`#D4AF37`)과 다른 fallback 9곳 제거 및 하드코딩 텍스트 색상 19곳을 변수로 치환.
- `[x]` **[WB-038]** **[컴포넌트]** 테마 폰트 선택적 로딩 및 커버 이미지 우선순위 (PR #47)
  - 연관: WB-030 / 완료일: 2026-08-04
  - 비고: 구글 폰트 9종 정적 로드를 선택된 1종만 런타임에 주입하도록 변경(`useThemeFont`). 커버 이미지(LCP)에 `fetchPriority="high"` 지정.
- `[x]` **[WB-037]** **[인프라/공통]** 카카오 SDK 온디맨드 로딩 및 타입 통합 (PR #46)
  - 연관: WB-006, WB-016 / 완료일: 2026-08-04
  - 비고: `index.html`의 동기 스크립트 2개(지도/공유)를 실제 사용 시점에 주입하도록 변경(`kakaoSdk.ts`). 3개 파일에 중복 선언되어 있던 `declare global { kakao: any }`를 `types/kakao.d.ts`로 통합.
- `[x]` **[WB-034]** **[인프라/공통]** 카카오 JS SDK 무결성 검증(SRI) 복원 (PR #43)
  - 연관: - / 완료일: 2026-08-01
  - 비고: 기존 integrity 값이 실제 파일 해시와 달라 스크립트가 차단되던 문제. 실제 파일에서 산출한 해시로 교체.
- `[x]` **[WB-033]** **[인프라/공통]** 링크 미리보기용 청첩장별 OG 태그 (Edge Middleware) (PR #42)
  - 연관: - / 완료일: 2026-08-01
  - 비고: 크롤러 UA에만 개입하며 실패 시 통과(fail-open). 라이브러리 `@vercel/edge` 추가.
- `[x]` **[WB-032]** **[데이터/API]** 하객 뷰 에러 원인 분리 및 재시도 버튼 추가 (PR #41)
  - 연관: WB-010 / 완료일: 2026-08-01
  - 비고: `PGRST116`(없음)과 일시적 장애를 구분. DB 일시정지 시 "주소가 틀렸다"고 안내하던 문제 해결.
- `[x]` **[WB-031]** **[인프라/공통]** Supabase 자동 일시정지 방지 워크플로우 (PR #39)
  - 연관: WB-002 / 완료일: 2026-08-01
  - 비고: 하루 1회 읽기 요청. 시크릿 이름 호환성은 WB-040에서 수정. **8/11 실행에서 DNS 실패 재발 — 상단 Handoff의 긴급 항목 참고.**
- `[x]` **[WB-030]** **[컴포넌트]** 청첩장 테마 폰트 선택 기능 (PR #34)
  - 연관: - / 완료일: 2026-07-26
  - 비고: 기록 누락분 소급 반영. 뷰어 매핑 누락 버그는 PR #35에서 수정됨.

### ✅ 완료 (Phase 4: 아키텍처 리팩토링 및 신규 컴포넌트 추가)
- `[x]` **[WB-028]** **[어드민]** RSVP 참석자 명단 및 방명록 관리 대시보드 화면 추가 (`AdminDetail.tsx`)
  - 연관: WB-012 / 완료일: 2026-07-31
- `[x]` **[WB-025]** **[컴포넌트]** 예식일 감성 달력(Calendar) 컴포넌트 추가
  - 연관: - / 완료일: 2026-07-31
- `[x]` **[WB-024]** **[어드민/빌더]** 빌더 폼 상태 단일화 및 입력 컴포넌트 분리 최적화 (PR #30)
  - 연관: - / 완료일: 2026-07-22
- `[x]` **[WB-022]** **[인프라/공통]** 데이터 로딩 스켈레톤(Skeleton) UI 적용
  - 연관: - / 완료일: 2026-07-31

### ✅ 완료 (Phase 3: 고도화 및 최적화)
- `[x]` **[WB-023]** **[컴포넌트]** 플로팅 액션 버튼 (맨 위로 가기) 추가 (PR #29)
  - 연관: - / 완료일: 2026-07-21
- `[x]` **[WB-021]** **[컴포넌트]** 갤러리 스와이프(슬라이더) 기능 고도화 (PR #28)
  - 연관: - / 완료일: 2026-07-21
- `[x]` **[WB-018]** **[컴포넌트]** 커버 영역 D-Day 카운터 기능 추가 (PR #27)
  - 연관: - / 완료일: 2026-07-21

### ✅ 완료 (Phase 2: 어드민 기초 구축 완료)
- `[x]` **[WB-017]** **[어드민/빌더]** 청첩장 수정(Edit) 모드 활성화 및 버그 수정 (PR #24)
  - 연관: - / 완료일: 2026-07-21
- `[x]` **[WB-016]** **[하객 뷰]** 카카오톡 공유 API(JS SDK) 연동 및 썸네일 (PR #25)
  - 연관: - / 완료일: 2026-07-21
- `[x]` **[WB-015]** **[어드민/빌더]** 빌더 데이터와 뷰어 데이터 정합성 검증 및 동기화 (PR #22)
  - 연관: - / 완료일: 2026-07-19
  - 비고: 빌더 폼 항목(계좌번호, 인사말, 갤러리, 혼주 등) 대폭 확장 및 TS 타입 교정.
- `[x]` **[WB-014]** **[어드민/빌더]** 어드민 페이지 모바일 반응형 처리 (PR #20)
  - 연관: WB-013 / 완료일: 2026-07-19
- `[x]` **[WB-013]** **[어드민/빌더]** 청첩장 빌더(InvitationBuilder) 폼 구현 및 DB 인서트 (PR #19)
  - 연관: - / 완료일: 2026-07-19
- `[x]` **[WB-012]** **[어드민/빌더]** 관리자 대시보드(AdminDashboard) 스켈레톤 구축 (PR #18)
  - 연관: - / 완료일: 2026-07-19
- `[x]` **[WB-011]** **[어드민/빌더]** 관리자 로그인(AdminLogin) 페이지 구축 (PR #18)
  - 연관: - / 완료일: 2026-07-19

### ✅ 완료 (Phase 1: 뼈대 구축 완료)
- `[x]` **[WB-009]** **[컴포넌트]** 모시는 글(Greeting) 타이포그래피 및 UI 완성 (PR #26)
  - 연관: - / 완료일: 2026-07-21
  - 비고: 세리프 폰트 적용, 혼주 관계 표기 로직 구현.
- `[x]` **[WB-010]** **[데이터/API]** 청첩장 뷰어 동적 데이터(Supabase) 패치 연동 (PR #16)
  - 연관: - / 완료일: 2026-07-19
- `[x]` **[WB-008]** **[컴포넌트/API]** 방명록(Guestbook) UI 구현 및 커스텀 훅 완성 (PR #15)
  - 연관: WB-002 / 완료일: 2026-07-18
- `[x]` **[WB-007]** **[컴포넌트]** 마음 전하실 곳(Account) 아코디언 및 송금 딥링크 연동
  - 연관: - / 완료일: 2026-07-15
- `[x]` **[WB-006]** **[컴포넌트]** 오시는 길(Location) 카카오맵 SDK 및 딥링크 연동
  - 연관: - / 완료일: 2026-07-15
- `[x]` **[WB-002]** **[데이터/API]** Supabase 초기 환경 설정 (프로젝트 키 연동)
  - 연관: - / 완료일: 2026-07-12
- `[x]` **[WB-005]** **[컴포넌트]** 이미지 갤러리(Gallery) 상세 모달 및 Stagger 모션 구현
  - 연관: WB-004 / 완료일: 2026-07-11
- `[x]` **[WB-004]** **[컴포넌트]** 주요 섹션(Greeting, Gallery, Location, Account, Guestbook) UI 뼈대 생성
  - 연관: - / 완료일: 2026-07-10
- `[x]` **[WB-003]** **[컴포넌트]** 청첩장 메인 커버(Cover) UI 뼈대 개발
  - 연관: - / 완료일: 2026-07-10
- `[x]` **[WB-001]** **[인프라/공통]** 프론트엔드 라우팅 및 폴더 구조 스캐폴딩
  - 연관: - / 완료일: 2026-07-09

---

## 🏗️ 4. 프로젝트 결정 사항 (Project Decisions)

*이 영역은 아키텍처/기획의 중요한 결정 사항이 변경될 때마다 업데이트하여, 코딩 전 에이전트가 참고하는 컨텍스트 주입기로 사용합니다.*

- **모바일 퍼스트 레이아웃**: 모든 화면은 최대 폭 480px의 컨테이너(`Layout.tsx`) 내부에서 렌더링됨. 어드민 페이지 역시 모바일 환경에서도 제어 가능하도록 반응형이 적용됨.
- **스타일링 원칙**: Tailwind 등 유틸리티 프레임워크를 배제하고, CSS Variables 기반 Vanilla CSS 사용. 우아하고 따뜻한 톤 유지.
- **동적 라우팅**: 개별 청첩장은 `/:invitationSlug` URL 패턴을 사용하며, 해당 파라미터 기준으로 DB 패치 수행.
- **백로그 포맷**: Markdown Table이 아닌 계층형 리스트 방식을 채택하여 상태(대기/진행/완료) 별로 구분 관리 (Git 충돌 방지 목적).
- **링크 미리보기는 Edge Middleware 담당**: SPA라서 크롤러가 `index.html`의 고정 OG 태그만 읽는 한계가 있음. 루트의 `middleware.ts`가 **크롤러 UA일 때만** 청첩장별 태그를 생성해 응답하며, 실제 하객 요청은 DB 조회 없이 통과시킴. 어떤 실패든 통과(fail-open) 처리하여 미리보기 문제가 청첩장 접속을 막지 않도록 함. 이 파일은 매 요청 경로에 있으므로 수정 시 fail-open 원칙을 반드시 유지할 것.
- **외부 스크립트는 SRI 필수**: 어드민 로그인이 하객 페이지와 같은 오리진이고 인증 세션이 브라우저에 보관되므로, CDN 스크립트에는 `integrity`와 `crossorigin`을 유지함. SDK 버전을 올릴 때는 **실제 파일에서 해시를 다시 산출**할 것. 값을 추측해 넣으면 브라우저가 스크립트를 통째로 차단함(과거 실제 발생).
- **Supabase 무료 플랜 제약**: 7일간 DB 활동이 없으면 프로젝트가 자동 일시정지되고 복구가 수동임. `.github/workflows/supabase-keepalive.yml`이 하루 1회 읽기로 타이머를 리셋하되, 레포에 60일간 커밋이 없으면 GitHub이 스케줄을 비활성화하는 한계가 있음. 시크릿 이름은 `SUPABASE_URL`/`VITE_SUPABASE_URL` 둘 다 인식함(WB-040). 실제 예식 기간에는 유료 플랜 검토 필요.
- **하객 화면과 어드민 화면의 Supabase 클라이언트를 분리**: 하객 화면(`src/hooks/*`)은 `src/services/db.ts`(데이터 조회 전용, `postgrest-js`)를, 로그인이 필요한 어드민 화면(`src/pages/admin/*`)은 `src/services/supabase.ts`(auth 포함 전체 클라이언트, `supabase-js`)를 사용함. 하객 화면 코드에서 `supabase.ts`를 import하면 auth·스토리지·실시간 모듈이 초기 번들에 포함되어 무거워지므로 절대 섞지 말 것.
- **외부 SDK는 온디맨드 로딩**: 카카오 지도/공유 SDK는 `index.html`에 정적으로 넣지 않고 `src/services/kakaoSdk.ts`에서 실제 사용 시점(지도는 스크롤 진입, 공유는 버튼 클릭)에 주입함. 새로운 외부 스크립트를 추가할 때도 이 패턴을 따를 것.
- **번들 청크 전략과 의존성 기록 규칙**: `vite.config.ts`의 `manualChunks`로 `vendor-react`/`vendor-framer`/`vendor-supabase`를 분리하고, `InvitationPage.tsx`에서 상단 섹션 이후 컴포넌트를 `React.lazy`로 지연 로딩함. **새 의존성을 추가하면 반드시 `package.json`에 기록할 것** — PR #50에서 `vite.config.ts`에 import만 추가하고 `package.json`을 갱신하지 않아 로컬에는 있던 패키지가 Vercel의 클린 설치(`npm ci`)에서 빠져 배포가 깨진 사고가 실제로 있었음(PR #51에서 긴급 수정).
- **Node.js 22 이상 필요**: Vercel 배포와 `package.json`의 `engines`가 Node 22 기준으로 고정됨(`.github/workflows/deploy.yml` 포함). 로컬 개발 환경도 맞춰둘 것.
